var list = List();

var start = clock();
each (i : range(1000000 - 1)) list.add(i);

var sum = 0;
each(i : range(list.size() - 1)) {
  sum = sum + list[i];
}

print sum;
println("elapsed: ", clock() - start);
