// This is a regression test for a bug where inserting in a map would not
// correctly reuse tombstone entries, eventually deadlocking on insert.
var map = Map();

for (i = 0; i <= 100 ; i = i + 1) {
  map[i] = i;

  if (i >= 10) map.remove(i - 10);
}

print map.size(); // expect: 10
